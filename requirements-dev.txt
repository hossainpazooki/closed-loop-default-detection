# Provenance environment for the committed artifacts + frozen byte-identity
# baseline: Python 3.14.2, scikit-learn 1.9.0. scikit-learn and numpy are pinned
# because HistGradientBoosting float output shifts across releases.
#
# REQUIRES PYTHON >= 3.11: the numpy==2.4.6 / scikit-learn==1.9.0 pins below both
# declare Requires-Python >=3.11, so `pip install -r requirements-dev.txt` will NOT
# resolve on Python 3.10 (the CI `pinned-repro` job runs Python 3.14). This file is
# only for reproducing the exact committed numbers. The LIBRARY itself supports
# Python >=3.10 (pyproject `requires-python`, CI-tested on 3.10-3.13); on 3.10 use
# the range install instead:  pip install -e ".[dev]"
numpy==2.4.6
pandas>=2.2
scikit-learn==1.9.0
scipy>=1.11
matplotlib>=3.8
pytest>=8.0
