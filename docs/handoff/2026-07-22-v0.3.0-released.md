# Handoff — v0.3.0 released; v3 closed out

*2026-07-22. Newest commit described: **`1ab178b`** (`chore: release 0.3.0, drop
author email from metadata`), tagged **`v0.3.0`**. Measure drift from here.
Reader is assumed to know the cldd foundations (selective-labels harness,
determinism contract, levers); this brief carries the v3 layer and the release.*

This session picked up the 2026-07-20 brief, re-verified its load-bearing
claims (all held), and ran the 0.3.0 release to completion. **0.3.0 is live on
PyPI and effect-verified from a clean consumer venv** — not from the workflow's
success log.

## Current state

- **[released + effect-verified]** **0.3.0 on PyPI** (wheel + sdist). Verified by
  probing the effect, not the report: fresh venv → `pip install
  closed-loop-default-detection` resolved **0.3.0**, imported, and the
  discriminating probe passed — `inspect.signature(FeedbackLoop)` contains
  `retrain`/`policy_mode`, which 0.2.0 provably lacks (the same non-vacuity
  fact the identity test's guard rests on), so the probe cannot pass against
  the effect-absent state. `py.typed` ships; trusted publisher carried over,
  no re-registration was needed.
  *re-verify:* `curl -s https://pypi.org/pypi/closed-loop-default-detection/json | python -c "import json,sys;print(json.load(sys.stdin)['info']['version'])"` (expect `0.3.0`)
- **[released]** Release metadata: `CHANGELOG.md` 0.3.0 header dated 2026-07-22
  and extended to record what 0.3.0 *actually* ships (54 new tests / 203 total,
  the differential identity gate — the entry previously predated the CI fix);
  `CITATION.cff` at 0.3.0 / 2026-07-22.
  *re-verify:* `grep -n "0.3.0" CHANGELOG.md CITATION.cff | head -4`
- **[released]** **Author email removed from package metadata at 0.3.0** —
  dropped from `pyproject.toml` authors and `CITATION.cff`; 0.3.0's PyPI
  `author_email` is `None`. It remains immutably in 0.1.0/0.2.0 metadata — see
  Locked decisions.
  *re-verify:* `curl -s https://pypi.org/pypi/closed-loop-default-detection/0.3.0/json | python -c "import json,sys;print(json.load(sys.stdin)['info']['author_email'])"`
- **[verified]** CI green on `ec92b43` (all 15 jobs) and on the release-prep
  commit per operator; green re-checked for vacuity on `ec92b43`:
  `pinned-repro` 194 passed / 9 skipped (= 203; skips are the 9 private-data
  fidelity tests), each compat job 188 + 9 (= 197, the `-m "not pinned"`
  subset). The identity gate executed on Linux, it did not skip.
  *re-verify:* `gh run list --limit 1 --json headSha,conclusion`
- **[verified]** Local gates at release: full suite **203 passed** under pins;
  `sphinx -b html -W` clean; wheel builds as 0.3.0 with `cldd/py.typed` and no
  `Author-email` in METADATA. Editable-install metadata refreshed — now
  reports **0.3.0** (was stale at 0.1.0; that 07-20 learning is resolved).
  *re-verify:* `.venv/Scripts/python.exe -m pytest -q`
- **[verified]** v3 science unchanged, recomputed this session from
  `artifacts/feedback_profit_sweep.csv` via the only sanctioned source: **H1
  CONFIRMED** (median −0.0028, 24/25 seeds, Holm 2.325e-06, clears the 0.0018
  prior-arm-MAD floor); **H2 opposite-sign** (+0.0119, 0/25); **H3/H3a
  negative**; **H4 identity exact** (max abs error 1e-10, 300 checks × 3
  severities). Note the artifact filename: earlier briefs shorthanded it as
  `feedback_sweep.csv`; the real file (and what the stats script reads) is
  `feedback_profit_sweep.csv`.
  *re-verify:* `.venv/Scripts/python.exe scripts/feedback_sweep_stats.py`
- **[built, UNCOMMITTED]** This brief, two new learnings entries
  (`2026-07-22-*`), and the index rows. Commit command is with the operator.
- **[not started]** Spec §9 deferred work: Option A (`unobserved_strength` ×
  severity frontier surface — the assessment's staked "cheapest most-valuable
  move", natural v4) and Option C (structured/segmented selection policies).

## Locked decisions

All 07-20 locks hold unchanged (differential identity comparison, no `pinned`
marker on the identity test, tolerances never loosened, H2 reported as
measured, dated docs stay dated, `TARGET_DECLINED_ECE` a registered defect,
git history belongs to the operator). New this session:

- **Old-release metadata stays: deletion/restart rejected (2026-07-22).** The
  author email in 0.1.0/0.2.0 PyPI metadata is immutable and already mirrored;
  deleting releases would break pins without reaching mirrors, and deleting
  the project would free the name, burn all used filenames forever, and
  destroy the trusted-publisher registration. Scrub is forward-only — 0.3.0+
  is clean. *Reason to check:* holds unless PyPI's immutability/mirroring
  model changes, i.e. effectively always.
- **`CITATION.cff` tracks the released version** (0.3.0 / 2026-07-22) — this
  repo's existing pattern, now exercised twice; flip it only at the next
  release.

## Reuse map

Unchanged from 07-20 (all re-verified as still present this session):

- `scripts/feedback_sweep_stats.py` — the **only** source for v3 figures;
  reads `artifacts/feedback_profit_sweep.csv`, exits non-zero if H4 fails.
- `_load_pre_v3_feedback`, `pre_v3_feedback`/`baseline_hash` fixtures,
  `_serialize_v1_fields`/`_hash_v1_fields` (`tests/test_feedback_flags.py`) —
  the cross-version identity machinery; extend the fixtures, never re-run the
  baseline loop ad hoc, never store a new constant.
- Docs slots: `docs/how-it-works.md` (mechanics), `docs/validation.md`
  (gates), `docs/configuration.md` (knobs); long-form never returns to README.

New this session:

- **Release-verification pattern** (see `docs/learnings/2026-07-22-*`): verify
  a PyPI release against the simple index or a clean-venv install with a
  discriminating probe (a symbol the previous version lacks), never the
  project-level JSON alone — it is CDN-cached and lagged this very release.

## Invariants

Unchanged from the 07-20 brief; restated because a newcomer would otherwise
violate them:

- Never modify `artifacts/*.csv`, frozen determinism baselines, or headline
  claims. Byte-determinism per seed; all randomness through seeded
  `numpy.random.Generator` streams (levers seed from `ctx`).
- `CalibratedPDClassifier.predict_proba(X)[:, 1]` byte-identical to
  `train_pd_model(...).predict_pd(X)` (test-enforced).
- **No unmarked float-exact-against-a-constant test, ever** — that broke CI for
  a day pre-0.3.0. Float-exact ⇒ differential, or marked `pinned`.
- `fetch-depth: 0` stays on every CI job that runs pytest; without it the
  identity gate skips and green goes vacuous. When touching those tests,
  re-check the skip count (9 is right; 12 means the gate stopped running).
- Gates: `pytest` (203 under pins; 194+9 on public CI), coverage, `sphinx -b
  html -W` (0 warnings; no mermaid in `docs/*.md`; new `docs/` subdirs must be
  Sphinx-excluded), wheel ships `cldd/py.typed`.
- `_check_sample_weight` is private sklearn API — watch on sklearn major bumps.

## Open / next

1. **Commit this brief + the two learnings entries + index rows** (untracked;
   operator's command). Nothing else is uncommitted except the long-standing
   `docs/superpowers/plans/2026-07-14-cldd-v3-feedback-profit.md` — still the
   operator's call whether the v3 plan ships as provenance.
2. **No release work remains.** 0.3.0 is closed end-to-end. The next
   substantive work is spec §9: **Option A** (frontier surface, natural v4) or
   **Option C** (structured selection policies). Option A is the staked
   priority; nothing blocks it.
3. **Optional, cheap, still open from 07-20:** restore a local Linux loop
   (Docker Desktop or WSL `python3.12-venv`) so the next platform-specific CI
   failure doesn't cost a push cycle.
